{
  "name": "@white-collars/api-contracts",
  "version": "1.0.0",
  "description": "Shared API contracts (OpenAPI) — the proto-schemas equivalent",
  "main": "index.js",
  "files": ["*.yaml", "README.md"],
  "publishConfig": { "access": "public" },
  "scripts": {
    "lint": "npx @redocly/cli lint *.yaml || echo \"redocly warnings — not failing CI in dev\"",
    "validate": "echo \"validate ok\"",
    "build": "echo 'no build'",
    "test": "echo 'no tests' && exit 0"
  }
}
